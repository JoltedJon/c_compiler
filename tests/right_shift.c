unsigned int right_shift(unsigned value, unsigned num_bits) {
  int result = 0;
  int counter = 32;
  while (counter != 0) {
    if (num_bits + counter) unsigned result_bit_mask = ;
  }

  return result;
}